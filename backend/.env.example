# Copy this file to `.env` (this repo ignores `.env`).

# Local MySQL example:
DATABASE_URL=mysql+pymysql://root:REPLACE_ME@127.0.0.1:3306/shiftscore

# SQLite fallback (no MySQL needed):
# DATABASE_URL=sqlite+pysqlite:///./dev.db
